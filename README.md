# FinanceList

财富侦探 mock app。页面放在 `app` 路由下，并为每个页面保留相邻的 `page.md` 说明文件。

页面入口：

- `/`
- `/buy-rent`
- `/investment`
- `/living-cost`
- `/retirement`
- `/personal-asset`

当前阶段只保留前端 mock 页面；API 与数据库实现后续重新设计。
